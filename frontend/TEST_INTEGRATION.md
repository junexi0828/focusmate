# 프론트엔드 연동 테스트 가이드

## 현재 상태

✅ **백엔드**: 정상 작동 (localhost:8000)
✅ **프론트엔드**: 실행 중 (localhost:3000)
✅ **API 응답**: 새 필드 포함 (friend_last_seen_at, friend_status_message)
✅ **코드 업데이트**: 완료

## 브라우저에서 확인 방법

### 1. 강제 새로고침
브라우저 캐시를 무시하고 새로고침:
- **Windows/Linux**: `Ctrl + Shift + R` 또는 `Ctrl + F5`
- **Mac**: `Cmd + Shift + R`

### 2. 개발자 도구 확인
1. 브라우저에서 `F12` 눌러 개발자 도구 열기
2. **Network** 탭으로 이동
3. "Disable cache" 체크박스 활성화
4. 페이지 새로고침

### 3. 친구 목록 확인
1. 로그인: user1@test.com / password123
2. `/friends` 페이지 접속
3. 친구 카드에서 확인할 내용:
   - ✅ 온라인 상태 표시 (녹색 점)
   - ✅ "온라인" 또는 "마지막 접속: X분 전" 텍스트
   - ✅ 상태 메시지 (설정된 경우)
   - ✅ "메시지" 버튼 (클릭 가능)

### 4. API 응답 확인
개발자 도구 > Network 탭에서:
1. `friends/` 요청 찾기
2. Response 탭에서 JSON 구조 확인:
```json
{
  "friends": [{
    "friend_username": "심동혁",
    "friend_is_online": false,
    "friend_last_seen_at": null,
    "friend_status_message": null
  }]
}
```

## 테스트 시나리오

### 시나리오 1: 친구 목록 보기
1. `/friends` 접속
2. 친구가 표시되는지 확인
3. 온라인/오프라인 상태 표시 확인

### 시나리오 2: 친구와 채팅
1. 친구 카드에서 "메시지" 버튼 클릭
2. 토스트 메시지 확인: "채팅방이 생성되었습니다"

### 시나리오 3: 친구 검색
1. 검색창에 친구 이름 입력 (예: "심")
2. 필터링된 결과 확인

### 시나리오 4: 온라인 필터
1. "온라인만" 버튼 클릭
2. 온라인 친구만 표시되는지 확인

## 트러블슈팅

### 변경사항이 보이지 않는 경우
1. **하드 리프레시**: Cmd/Ctrl + Shift + R
2. **캐시 삭제**:
   - Chrome: Settings > Privacy > Clear browsing data
   - 또는 시크릿 모드로 테스트
3. **개발 서버 재시작**:
   ```bash
   cd frontend
   # Ctrl+C로 중지 후
   npm run dev
   ```

### API 에러 발생 시
1. 백엔드 서버 실행 확인:
   ```bash
   curl http://localhost:8000/health
   ```
2. 토큰 만료 시 로그아웃 후 재로그인

### "친구가 없습니다" 표시 시
데이터 확인:
```bash
cd backend
source venv/bin/activate
python -c "
import asyncio
from app.infrastructure.database.session import get_db_session
from sqlalchemy import text

async def check():
    async with get_db_session() as db:
        result = await db.execute(text('SELECT COUNT(*) FROM friend'))
        print(f'Friends count: {result.scalar()}')

asyncio.run(check())
"
```

## 실제 사용 예시

### 현재 테스트 데이터
- **user1** (김도윤): user1@test.com / password123
- **user3** (심동혁): user3@test.com / password123
- user1과 user3는 친구 관계

### 테스트 순서
1. user1으로 로그인
2. `/friends` 접속
3. 심동혁 친구 카드 확인
4. 온라인 상태: "오프라인" (아무도 접속 안 함)
5. "메시지" 버튼 클릭하여 채팅 생성

## 예상 결과

### 친구 카드 UI
```
┌──────────────────────────────────────┐
│  ●  심동혁                          │
│     ● 오프라인                       │
│                    [메시지] [삭제]    │
└──────────────────────────────────────┘
```

### 온라인 상태일 때
```
┌──────────────────────────────────────┐
│  ●  심동혁                          │
│  🟢 온라인                           │
│     "공부 중입니다"                   │
│                    [메시지] [삭제]    │
└──────────────────────────────────────┘
```

## 추가 기능 (구현 완료)

이 컴포넌트들은 생성되었지만 아직 라우트에 연결되지 않았습니다:

### InvitationCodeDialog
```typescript
import { InvitationCodeDialog } from '../features/chat/components/InvitationCodeDialog';

// 사용법
<InvitationCodeDialog
  roomId="room-id"
  mode="generate" // or "join"
  onClose={() => {}}
  onSuccess={(roomId) => {}}
/>
```

### CreateRoomWithFriends
```typescript
import { CreateRoomWithFriends } from '../features/chat/components/CreateRoomWithFriends';

// 사용법
<CreateRoomWithFriends
  onClose={() => {}}
  onSuccess={(roomId) => {}}
/>
```

이 컴포넌트들을 사용하려면 해당 페이지에서 import하고 state로 다이얼로그를 열면 됩니다.
